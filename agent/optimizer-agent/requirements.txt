# optimizer-agent has NO pip dependencies — it is pure Python stdlib
# (urllib.request talks to the Lemonade OpenAI-compatible endpoint; no
#  requests / openai package needed). Python 3.10+.
#
# External RUNTIME requirements (not pip-installable — provided by the system):
#   - A Lemonade OpenAI-compatible server hosting a tool-calling model
#     (default endpoint http://localhost:13305/v1). https://github.com/lemonade-sdk/lemonade
#   - ROCm / HIP toolchain on PATH for the benchmark tools: hipcc, rocminfo,
#     llvm-mc (ISA census). Tested on ROCm 7.x, gfx1201 (Radeon AI PRO R9700).
#   - Two AMD GPUs recommended: one for the brain, one isolated for benchmarks
#     (single-GPU works but brain + bench then share the card).
